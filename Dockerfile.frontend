# Frontend Development Dockerfile
# Hot-reloading Vite dev server for local development

FROM node:20-slim

WORKDIR /app

# Copy package files (source files come from volume mount)
COPY package*.json ./

# Vite dev server port
EXPOSE 3000

# Install dependencies at container start, then run Vite
# This ensures deps are always in sync with package.json
CMD ["sh", "-c", "npm install && npm run dev -- --host 0.0.0.0 --port 3000"]
